export interface Input {
  label: string;
  type: string;
}

const clientInput = [
  { label: "E-mail", type: "email" },
  { label: "Senha", type: "password" },
];

const inputInfos = { clientInput };

export { inputInfos };
